specVersion: "v1beta"
serviceId: "ai-trip-planner"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "ai-trip-planner-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
